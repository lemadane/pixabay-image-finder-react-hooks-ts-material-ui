import React, { FC } from 'react';

const initState = {
   searchText: '',
   amount: 15,
   images: [],
}

const Search: FC = () => {
   
   return (
      <div>

      </div>
   );
};

export default Search;
